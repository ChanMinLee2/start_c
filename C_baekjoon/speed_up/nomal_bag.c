#include <stdio.h>
#define MAX 100

typedef struct nomal_bag bag;
struct bag
{
    int weight;
    int values;
};


int main(void)
{
    int number = 0, keep = 0;
    scanf("%d, %d", &number, &keep);

    for(int i = 0; i < number; i++)
    {
        
    }
}